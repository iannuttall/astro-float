Text with \*stars\*, \_underscores\_, a \[bracket\], a \<tag> and a back\\slash.

\# not a heading, and 1. mid-sentence is not a list.

A snake_case_word keeps its underscores; \_lonely\_ ones are escaped.
